# 4DGaussians runtime deps WITHOUT torch/torchvision (installed separately
# from the CUDA 12.8 PyTorch index for RTX 5080 Blackwell support) and
# WITHOUT mmcv (replaced by a pure-Python mmengine shim since mmcv's
# from-source build fails on modern setuptools).
lpips==0.1.4
plyfile==1.1
pytorch_msssim==1.0.0
open3d==0.19.0
imageio[ffmpeg]==2.37.0
mmengine==0.10.7

# mmcv shim — 3-line re-export of mmengine.Config under the `mmcv` name, so
# 4DGaussians' `import mmcv; mmcv.Config.fromfile(...)` resolves without
# pulling in the real mmcv (which requires a broken from-source build).
-e ./vendor/mmcv_shim
